<h1>Editing Book</h1>
<%= form_with model: @book do |f| %>
  <%= f.label "Title", :title %>
  <%= f.text_field :title %>
  <%= f.label "Opinion", :body %>
  <%= f.text_area :body %>
  <%= f.submit %>
<% end %>
<%= link_to "Show", book_path(@book) %> | <%= link_to "Back", books_path %>