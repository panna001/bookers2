<% if user_signed_in? %>
  <%= form_with url: search_path, method: :get, local: true do |f| %>
    <%= f.text_field :key %>
    <%= f.select :model, ["Book", "User"] %>
    <%= f.select :search, ["完全一致","部分一致","前方一致","後方一致"] %>
    <%= f.submit "検索" %>
  <% end %>
<% end %>